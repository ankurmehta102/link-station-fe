export type LoginFormValues = {
  email: string;
  password: string;
  // rememberMe: boolean;
};

export type SignupFormValues = {
  firstName: string;
  lastName: string;
  username: string;
  email: string;
  password: string;
  confirmPassword: string;
};
